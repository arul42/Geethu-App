import React from 'react';
import '../App.css';

const healthy= (props) =>
{
    return(
    <div className ='Sheer'>
    <p> the shimla special {props.name} available here at {props.price}</p>
    </div>
    )
}
export default healthy;